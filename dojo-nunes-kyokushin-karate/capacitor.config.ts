import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dojo-nunes-kyokushin-karate',
  webDir: 'www'
};

export default config;
